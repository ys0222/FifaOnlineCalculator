const house = () => {
let price = prompt('숫자를 입력하시오')
let result : number = +price * 0.6
alert(`${result}`)
}   

const house10 = () => {
    let price = prompt('숫자를 입력하시오')
    let firstPrice = +price * 0.6
    let fee : number = +price * 0.4
    let discount : number = +fee * 0.1
    let result : number = +firstPrice + discount
    alert(`${result}`)
    }   

const house20 = () => {
    let price = prompt('숫자를 입력하시오')
    let firstPrice = +price * 0.6
    let fee : number = +price * 0.4
    let discount : number = +fee * 0.2
    let result : number = +firstPrice + discount
    alert(`${result}`)
    }  

const house30 = () => {
    let price = prompt('숫자를 입력하시오')
    let firstPrice = +price * 0.6
    let fee : number = +price * 0.4
    let discount : number = +fee * 0.3
    let result : number = +firstPrice + discount
    alert(`${result}`)
    }  

//집

const TC = () => {
    let price = prompt('숫자를 입력하시오')
    let result : number = +price * 0.6
    alert(`${result}`)
    }   
        
const TC10 = () => {
    let price = prompt('숫자를 입력하시오')
    let firstPrice = +price * 0.6
    let fee : number = +price * 0.4
    let discount : number = +fee * 0.2
    let coupondiscouint = fee * 0.1
    let result : number = +firstPrice + discount + coupondiscouint
    alert(`${result}`)
    }   
        
const TC20 = () => {
    let price = prompt('숫자를 입력하시오')
    let firstPrice = +price * 0.6
    let fee : number = +price * 0.4
    let discount : number = +fee * 0.2
    let coupondiscouint = fee * 0.2
    let result : number = +firstPrice + discount + coupondiscouint
    alert(`${result}`)
    }  

const TC30 = () => {
    let price = prompt('숫자를 입력하시오')
    let firstPrice = +price * 0.6
    let fee : number = +price * 0.4
    let discount : number = +fee * 0.2
    let coupondiscouint = fee * 0.3
    let result : number = +firstPrice + discount + coupondiscouint
    alert(`${result}`)
    }  

//탑클

const PC = () => {
    let price = prompt('숫자를 입력하시오')
    let firstPrice : number = +price * 0.6
    let fee : number = +price  * 0.4
    let discount : number = +fee * 0.3
    let coupondiscouint = fee * 0.0
    let result : number = +firstPrice + discount + coupondiscouint 
    alert(`${result}`)
    }   
        
const PC10 = () => {
    let price = prompt('숫자를 입력하시오')
    let firstPrice = +price * 0.6
    let fee : number = +price * 0.4
    let discount : number = +fee * 0.3
    let coupondiscouint = fee * 0.1
    let result : number = +firstPrice + discount + coupondiscouint
    alert(`${result}`)
    }   
        
const PC20 = () => {
    let price = prompt('숫자를 입력하시오')
    let firstPrice = +price * 0.6
    let fee : number = +price * 0.4
    let discount : number = +fee * 0.3
    let coupondiscouint = fee * 0.2
    let result : number = +firstPrice + discount + coupondiscouint
    alert(`${result}`)
    }  

const PC30 = () => {
    let price = prompt('숫자를 입력하시오')
    let firstPrice = +price * 0.6
    let fee : number = +price * 0.4
    let discount : number = +fee * 0.3
    let coupondiscouint = fee * 0.3
    let result : number = +firstPrice + discount + coupondiscouint
    alert(`${result}`)
    }

//PC

const all = () => {
    let price = prompt('숫자를 입력하시오')
    let firstPrice : number = +price * 0.6
    let fee : number = +price  * 0.4
    let discount : number = +fee * 0.5
    let result : number = +firstPrice + discount
    alert(`${result}`)
    }   
        
const all10 = () => {
    let price = prompt('숫자를 입력하시오')
    let firstPrice = +price * 0.6
    let fee : number = +price * 0.4
    let discount : number = +fee * 0.5
    let coupondiscouint = fee * 0.1
    let result : number = +firstPrice + discount + coupondiscouint
    alert(`${result}`)
    }   
        
const all20 = () => {
    let price = prompt('숫자를 입력하시오')
    let firstPrice = +price * 0.6
    let fee : number = +price * 0.4
    let discount : number = +fee * 0.5
    let coupondiscouint = fee * 0.2
    let result : number = +firstPrice + discount + coupondiscouint
    alert(`${result}`)
    }  

const all30 = () => {
    let price = prompt('숫자를 입력하시오')
    let firstPrice = +price * 0.6
    let fee : number = +price * 0.4
    let discount : number = +fee * 0.5
    let coupondiscouint = fee * 0.3
    let result : number = +firstPrice + discount + coupondiscouint
    alert(`${result}`)
    }