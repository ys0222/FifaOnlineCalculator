var house = function () {
    var price = prompt('숫자를 입력하시오');
    var result = +price * 0.6;
    alert("".concat(result));
};
var house10 = function () {
    var price = prompt('숫자를 입력하시오');
    var firstPrice = +price * 0.6;
    var fee = +price * 0.4;
    var discount = +fee * 0.1;
    var result = +firstPrice + discount;
    alert("".concat(result));
};
var house20 = function () {
    var price = prompt('숫자를 입력하시오');
    var firstPrice = +price * 0.6;
    var fee = +price * 0.4;
    var discount = +fee * 0.2;
    var result = +firstPrice + discount;
    alert("".concat(result));
};
var house30 = function () {
    var price = prompt('숫자를 입력하시오');
    var firstPrice = +price * 0.6;
    var fee = +price * 0.4;
    var discount = +fee * 0.3;
    var result = +firstPrice + discount;
    alert("".concat(result));
};
//집
var TC = function () {
    var price = prompt('숫자를 입력하시오');
    var result = +price * 0.6;
    alert("".concat(result));
};
var TC10 = function () {
    var price = prompt('숫자를 입력하시오');
    var firstPrice = +price * 0.6;
    var fee = +price * 0.4;
    var discount = +fee * 0.2;
    var coupondiscouint = fee * 0.1;
    var result = +firstPrice + discount + coupondiscouint;
    alert("".concat(result));
};
var TC20 = function () {
    var price = prompt('숫자를 입력하시오');
    var firstPrice = +price * 0.6;
    var fee = +price * 0.4;
    var discount = +fee * 0.2;
    var coupondiscouint = fee * 0.2;
    var result = +firstPrice + discount + coupondiscouint;
    alert("".concat(result));
};
var TC30 = function () {
    var price = prompt('숫자를 입력하시오');
    var firstPrice = +price * 0.6;
    var fee = +price * 0.4;
    var discount = +fee * 0.2;
    var coupondiscouint = fee * 0.3;
    var result = +firstPrice + discount + coupondiscouint;
    alert("".concat(result));
};
//탑클
var PC = function () {
    var price = prompt('숫자를 입력하시오');
    var firstPrice = +price * 0.6;
    var fee = +price * 0.4;
    var discount = +fee * 0.3;
    var coupondiscouint = fee * 0.0;
    var result = +firstPrice + discount + coupondiscouint;
    alert("".concat(result));
};
var PC10 = function () {
    var price = prompt('숫자를 입력하시오');
    var firstPrice = +price * 0.6;
    var fee = +price * 0.4;
    var discount = +fee * 0.3;
    var coupondiscouint = fee * 0.1;
    var result = +firstPrice + discount + coupondiscouint;
    alert("".concat(result));
};
var PC20 = function () {
    var price = prompt('숫자를 입력하시오');
    var firstPrice = +price * 0.6;
    var fee = +price * 0.4;
    var discount = +fee * 0.3;
    var coupondiscouint = fee * 0.2;
    var result = +firstPrice + discount + coupondiscouint;
    alert("".concat(result));
};
var PC30 = function () {
    var price = prompt('숫자를 입력하시오');
    var firstPrice = +price * 0.6;
    var fee = +price * 0.4;
    var discount = +fee * 0.3;
    var coupondiscouint = fee * 0.3;
    var result = +firstPrice + discount + coupondiscouint;
    alert("".concat(result));
};
//PC
var all = function () {
    var price = prompt('숫자를 입력하시오');
    var firstPrice = +price * 0.6;
    var fee = +price * 0.4;
    var discount = +fee * 0.5;
    var result = +firstPrice + discount;
    alert("".concat(result));
};
var all10 = function () {
    var price = prompt('숫자를 입력하시오');
    var firstPrice = +price * 0.6;
    var fee = +price * 0.4;
    var discount = +fee * 0.5;
    var coupondiscouint = fee * 0.1;
    var result = +firstPrice + discount + coupondiscouint;
    alert("".concat(result));
};
var all20 = function () {
    var price = prompt('숫자를 입력하시오');
    var firstPrice = +price * 0.6;
    var fee = +price * 0.4;
    var discount = +fee * 0.5;
    var coupondiscouint = fee * 0.2;
    var result = +firstPrice + discount + coupondiscouint;
    alert("".concat(result));
};
var all30 = function () {
    var price = prompt('숫자를 입력하시오');
    var firstPrice = +price * 0.6;
    var fee = +price * 0.4;
    var discount = +fee * 0.5;
    var coupondiscouint = fee * 0.3;
    var result = +firstPrice + discount + coupondiscouint;
    alert("".concat(result));
};
